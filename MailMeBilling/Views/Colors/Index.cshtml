@model IEnumerable<MailMeBilling.Models.Color>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Colors)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Colorsdscription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Colors)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Colorsdscription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Colorid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Colorid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Colorid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
