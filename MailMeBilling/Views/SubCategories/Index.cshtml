@model IEnumerable<MailMeBilling.Models.SubCategory>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subcatagory)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Subcatagorydiscription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Subcatagory)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Subcatagorydiscription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Subcategoryid">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Subcategoryid">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Subcategoryid">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
