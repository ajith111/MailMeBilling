@model IEnumerable<MailMeBilling.Models.SubCategory>

@{
    ViewData["Title"] = "Subcategory List";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
    int count = 0;
}



<div class="col-md-12 mb-3">
    <div class="card text-left">
        <div class="card-body">
            <h4 class="card-title mb-3">SubCategory List</h4><br />
            <p>
                <a class="btn btn-primary" asp-action="Create">Add Subcategory</a>
            </p>

            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr class="text-center">
                            <th scope="col">S.No</th>
                            <th>
                                @Html.DisplayNameFor(model => model.Subcatagory)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Subcatagorydiscription)
                            </th>
                            <th scope="col">Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr class="text-center">
                                <th scope="row">@count</th>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Subcatagory)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Subcatagorydiscription)
                                </td>
                                <td>
                                    <a class="btn btn-primary" asp-action="Edit" asp-route-id="@item.Subcategoryid">Edit</a>
                                    <a class="btn btn-success" asp-action="Details" asp-route-id="@item.Subcategoryid">Details</a>
                                    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@item.Subcategoryid">Delete</a>

                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>